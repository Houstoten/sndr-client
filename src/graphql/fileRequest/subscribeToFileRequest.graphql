subscription subscribeToFileRequest{
    subscribeToFileRequest{
        id,
        senderid,
        sender {
            name,
            image
        }
        receiverid,
        name,
        size,
        accepted  
    }
}